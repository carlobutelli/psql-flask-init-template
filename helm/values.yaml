name: "loki"
fullname: "loki-identity-verification-service"

replicaCount: 3
imagePullSecret: regcred
namespace: default

image:
  repository: docker-builds.yellitech.io/loki-identity-verification-service
  tag: latest
  pullPolicy: Always

service:
  type: LoadBalancer
  port: 8080

resources: {}

configs: 
  FLASK_APP: loki.py

secrets:
  FLASK_SECRET_KEY: this-really-needs-to-be-changed